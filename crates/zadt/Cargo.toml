[package]
name = "zadt"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "ADT protocol support for the Ziege ABAP tooling framework"
repository = "https://github.com/kennyhml/ziege"

[[example]]
name = "transports"
required-features = ["reqwest"]

[[example]]
name = "object_creation"
required-features = ["reqwest"]

[[example]]
name = "users"
required-features = ["reqwest"]

[features]
default = ["reqwest"]
logging = ["dep:quick-xml", "dep:tracing"]
reqwest = ["dep:reqwest"]

[dependencies]
async-lock = "3.4"
async-trait = "0.1.88"
base64 = "0.22"
cookie = "0.18"
derive_builder = "0.20"
http = "1.3"
httparse = "=1.10.1"
quick-xml = { version = "0.41", optional = true }
reqwest = { version = "0.12", features = ["cookies", "native-tls"], optional = true }
secrecy = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde-xml-rs = "0.8.1"
std-uritemplate = "2.0"
thiserror = "2.0"
tracing = { version = "0.1", default-features = false, features = ["std"], optional = true }
url = "2.5"
uuid = { version = "1.18", features = ["v4"] }
zadt-macros = { version = "0.1.0", path = "../zadt-macros" }

[dev-dependencies]
httpmock = "0.8"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
